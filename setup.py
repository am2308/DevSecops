from setuptools import setup, find_packages

setup(
    name="background-remover-python-app",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "flask",   # add your real dependencies here
    ],
)
